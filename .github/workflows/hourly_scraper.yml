name: ⏸️ [DISABLED] Twilio Lead Scraper (Inactive)

# Scheduled Scraping is permanently disabled as per user instruction.
on:
  workflow_dispatch:

jobs:
  inactive:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "Twilio lead scraper cron schedule is permanently deactivated."
